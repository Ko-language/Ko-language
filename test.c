꾸러미 가연
가져오기 모션.가연
# [가게]{
   | 치명적인데 <- 0, 치코리타 |
   공용 | 치명 <- 3233 |
   | 바보 <- #[동아리] |
   가게(){
   ( 여기에변수를넣으면  != (1+1)) 이라면 계속 {
   		 치명적인데 <- "치명적"
    	치명적인데 <- "큭큭큭" + 메서드(#[객체](가.나.다))
    	( 중첩반복문  != (1+1)) 이라면 계속 {
    		!다시 위로
    		바보 <- "hello"
    	}을 반복
   }을 반복
   }
   메서드(죽어라){
    치명적인데 <- 죽어
   죽어라 !내보내기   
   }
}